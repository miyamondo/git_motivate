<p>検索</p>
<%= form_tag(diaries_searches_path,:method => 'get') do %>
  <%= text_field_tag :search %>
  <%= submit_tag 'Search', :name => nil %>
<% end %>

<ul>
<% @search_diaries.each do |search_diary| %>

  <div class="post_diarry">
    <header class="sub-hedder">
      <ul class="post-headding">
        <li><%= simple_time(search_diary.created_at) %></li>
        <li><%= search_diary.user.nickname %></li>
      </ul>
     <% if user_signed_in? && current_user.id == search_diary.user_id %>
    　 <!--<div class="more">-->
         <ul class="more_list">
           <li>
             <%= link_to "詳細", "/diaries/#{diary.id}", method: :get %>
           </li>
           <li>
             <%= link_to "編集", "/diaries/#{diary.id}/edit", method: :get %>
           </li>
           <li>
             <%= link_to "削除", "/diaries/#{diary.id}", method: :delete %>
           </li>
         </ul>
    　 <!--</div>-->
    　<% end %>
    </header>
   
    <div class="list">
      <p>人生の理念</p>
      <div class="post_philosophy">
        <%= simple_format(search_diary.philosophy) %>
      </div>
      <p>あなたの大事な目標</p>
      <div class="post_KPI">
        <%= simple_format(search_diary.KPI) %>
      </div>
       <p>その目標を達成するために何をした</p>
      <div class="for">
        <%= simple_format(search_diary.text1) %>
      </div>
      <p>明日もっと良くするために何をする</p>
      <div class="improvement">
        <%= simple_format(search_diary.text2) %>
      </div>
    </div>
  </div>
<% end %> <!--eachの終わり-->

</ul>